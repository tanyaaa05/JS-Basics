// Ternary Operator(Shortcut if-else):
let age = 23;

let status = (age >= 18) ? "Adult" : "Minor";
console.log("Status:", status);


let marks = 80;
let passedCriteria = (marks >= 35) ? "passed" : "Better Luck Next Time";
console.log("Result:", passedCriteria);

